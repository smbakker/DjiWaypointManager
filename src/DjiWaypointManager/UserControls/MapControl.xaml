<UserControl x:Class="DjiWaypointManager.UserControls.MapControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Mission Map" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>
            <Button Content="Fit to Mission" Margin="20,0,0,0" 
                    Command="{Binding FitToMissionCommand}"
                    Style="{StaticResource SecondaryButton}"/>
            <CheckBox Content="Show POIs" Margin="15,0,0,0" 
                      IsChecked="{Binding ShowPois}"/>
        </StackPanel>
        
        <Border x:Name="WebViewContainer" Grid.Row="1" Background="LightGray" BorderBrush="#CCC" BorderThickness="1">
            <TextBlock Text="Map will be loaded here..." HorizontalAlignment="Center" VerticalAlignment="Center" 
                       Foreground="#666" FontSize="16"/>
        </Border>
    </Grid>
</UserControl>