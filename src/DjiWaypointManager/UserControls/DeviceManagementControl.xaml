<UserControl x:Class="DjiWaypointManager.UserControls.DeviceManagementControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800">
    <Grid>
        <!-- Device Selection and Status Bar -->
        <Border Background="#F8F9FA" BorderBrush="#DEE2E6" BorderThickness="0,0,0,1" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Device Selection Label -->
                <TextBlock Grid.Column="0" Text="Device:" 
                           VerticalAlignment="Center" 
                           Margin="0,0,8,0" 
                           FontWeight="SemiBold"/>

                <!-- Device Selection ComboBox -->
                <ComboBox Grid.Column="1" 
                          ItemsSource="{Binding ConnectedDevices}"
                          SelectedItem="{Binding SelectedDevice}"
                          DisplayMemberPath="Name"
                          Margin="0,0,12,0">
                    <ComboBox.Style>
                        <Style TargetType="ComboBox">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ConnectedDevices.Count}" Value="0">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ComboBox.Style>
                </ComboBox>

                <!-- Mission Info -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="??" FontSize="14" Margin="0,0,5,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding MissionInfo}"
                               VerticalAlignment="Center" 
                               FontWeight="Bold" 
                               Foreground="#0078FF"/>
                </StackPanel>

                <!-- Device Status -->
                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="??" FontSize="12" Margin="0,0,5,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding DeviceStatusText}"
                               VerticalAlignment="Center" 
                               FontSize="11"
                               Foreground="#666"
                               MaxWidth="300"
                               TextTrimming="CharacterEllipsis"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>