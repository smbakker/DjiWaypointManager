<UserControl x:Class="DjiWaypointManager.UserControls.StatusBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="800">
    
    <StatusBar Height="25" Background="#F5F5F5" BorderBrush="#DDD" BorderThickness="0,1,0,0">
        <StatusBar.ItemsPanel>
            <ItemsPanelTemplate>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                </Grid>
            </ItemsPanelTemplate>
        </StatusBar.ItemsPanel>
        
        <!-- Main Status Text -->
        <StatusBarItem Grid.Column="0">
            <DockPanel>
                <!-- Scanning Progress Indicator -->
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" x:Name="ScanningIndicator" 
                           Visibility="{Binding IsScanningVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="??" Margin="0,0,5,0" FontSize="12" VerticalAlignment="Center"/>
                    <ProgressBar Width="100" Height="12" IsIndeterminate="True" Margin="0,0,10,0" VerticalAlignment="Center"/>
                </StackPanel>
                
                <!-- Status Text -->
                <TextBlock Text="{Binding StatusBarText}" VerticalAlignment="Center" Margin="5,0"/>
            </DockPanel>
        </StatusBarItem>
        
        <!-- Device Count -->
        <StatusBarItem Grid.Column="1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Devices: " FontSize="11" Foreground="#666" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding DeviceCount}" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>
        </StatusBarItem>
        
        <!-- Separator -->
        <Separator Grid.Column="2" Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="5,2"/>
        
        <!-- Connection Status -->
        <StatusBarItem Grid.Column="3">
            <StackPanel Orientation="Horizontal">
                <Ellipse Width="8" Height="8" Margin="0,0,5,0" VerticalAlignment="Center">
                    <Ellipse.Fill>
                        <SolidColorBrush Color="{Binding IsConnected, Converter={StaticResource BoolToColorConverter}}"/>
                    </Ellipse.Fill>
                </Ellipse>
                <TextBlock Text="{Binding ConnectionStatusText}" FontSize="11" VerticalAlignment="Center" Margin="0,0,10,0"/>
            </StackPanel>
        </StatusBarItem>
    </StatusBar>
</UserControl>